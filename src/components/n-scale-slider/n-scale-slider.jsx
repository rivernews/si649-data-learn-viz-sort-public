import React, { Component } from 'react'
import PropTypes from 'prop-types'

import "./n-scale-slider.module.scss"

export default class NScaleSlider extends Component {
  static propTypes = {
    prop: PropTypes,
    onSlideChang: PropTypes.func
  }

  render() {
    return (
      <div>
        
      </div>
    )
  }
}
